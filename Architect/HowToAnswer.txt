HowToAnswer.txt

如何回答架构一个软件
1. 需求，做什么，input output。 一次性的，长期运行的。 
100%正确，近似计算。特点，正确性，健壮性，扩展性，
2. 正确性，健壮性，扩展性，

设计。
逻辑视图（Logical View），设计的对象模型（使用面向对象的设计方法时）。
  模块，class。与现有系统的联系，接口，依赖。
  
过程视图（Process View），捕捉设计的并发和同步特征。
  运行，启动。多线程，多进程，集群。
物理视图（Physical View），描述了软件到硬件的映射，反映了分布式特性。
  网络，资源。OS，cpu，集群。 工具，比如update parameter。交互等。
  技术的应用，开发语言，docker。
开发视图（Development View），描述了在开发环境中软件的静态组织结构。
  cvs，编译等。开发语言，框架。 软件stack， 网络，数据，逻辑处理，view。

user case视图。


监控，报警，指标。log。安全，数据安全。加密。
测试，功能，性能。
维护，log清理。升级，修改参数。

软件工程
如何迭代，实施计划。人，工具，机器等。




